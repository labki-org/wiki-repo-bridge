# Copy to `.env` and fill in real values. .env is gitignored.
# The CLI auto-loads .env at startup so these are picked up by --bot-user /
# --bot-password flags without needing to source the file.

WIKI_REPO_BOT_USER=DAharoni@wiki-repo-bot
WIKI_REPO_BOT_PASSWORD=<paste bot password here>
